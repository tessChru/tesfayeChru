server:
  port: 8082

spring:
  application:
    name: order-service
  data:
    mongodb:
      uri: mongodb://root:password@localhost:27017/orderdb?authSource=admin
      database: orderdb

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
